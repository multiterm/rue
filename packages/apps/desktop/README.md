# @multiterm/rue-desktop

Thin Electron shell. Spawns `@multiterm/rue-core` as a sidecar; renderer loads `@multiterm/rue-webui`.

Modeled on `packages/desktop` in the opencode reference.

## Status

Phase 0 scaffold. The old desktop lives under `../archive/old-rue/desktop` for reference.

Phase 9 ports the Electron shell.

See `../docs/refactor-plan.md`.
