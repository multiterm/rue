import {useEffect,type ReactNode} from 'react'
import {cn} from '../_utils/cn.js'

export function Dialog({open,onClose,title,description,children,footer,className}:{open:boolean;onClose:()=>void;title:string;description?:string;children:ReactNode;footer?:ReactNode;className?:string}){useEffect(()=>{if(!open)return;const close=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose()};window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[onClose,open]);if(!open)return null;return <div className="fixed inset-0 z-50 grid place-items-center bg-overlay p-5 backdrop-blur-[2px]" role="presentation" onMouseDown={(event)=>{if(event.currentTarget===event.target)onClose()}}><section role="dialog" aria-modal="true" aria-labelledby="rue-dialog-title" aria-describedby={description?'rue-dialog-description':undefined} className={cn('w-full max-w-md rounded-2xl border border-border bg-elevated p-6 text-foreground shadow-[var(--rue-shadow-hard)]',className)}><header className="mb-6 flex items-start justify-between gap-6"><div><h2 id="rue-dialog-title" className="m-0 font-display text-xl font-bold tracking-tight">{title}</h2>{description&&<p id="rue-dialog-description" className="mb-0 mt-2 text-sm leading-6 text-muted">{description}</p>}</div><button type="button" aria-label="Close dialog" onClick={onClose} className="grid size-8 shrink-0 place-items-center rounded-full border-0 bg-surface-2 text-muted hover:text-foreground">×</button></header>{children}{footer&&<footer className="mt-7 flex justify-end gap-2 border-t border-border pt-5">{footer}</footer>}</section></div>}
