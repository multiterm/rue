/**
 * @multiterm/rue-ui — entry point.
 *
 * Will export SolidJS primitives (Button, Input, Dialog, ScrollArea, Tabs,
 * Switch, ModelPicker, MessageList, ToolCard, AttachmentChip, AskBar,
 * ChatHeader, SidebarSessionList, SettingsTabs), theme tokens, and JSON
 * themes ported from the opencode reference.
 *
 * Phase 0: scaffold only. Phase 6 lands the component library.
 *
 * See ../docs/refactor-plan.md.
 */
export const RUE_UI_VERSION = '0.0.0'
