#!/bin/sh
# rue-local-quality-pre-push
set -eu
root=$(git rev-parse --show-toplevel)
cd "$root"
[ "${RUE_SKIP_QUALITY:-0}" = "1" ] && { echo "Rue quality pre-push skipped"; exit 0; }
pnpm exec rune quality-static
